# Server settings
# Host-side port. Container always listens on 8000 internally.
PORT=8005

# Frontend dev server host-side port
FRONTEND_PORT=5173

# LLM provider API keys (set only the ones you use)
ANTHROPIC_API_KEY=